# HTTP、HTTPS 与 DNS

## 摘要

这一组内容关注用户一次访问行为背后的完整链路，包括域名解析、连接建立、TLS 握手、HTTP 请求响应以及浏览器渲染。重点不是背协议分层，而是看懂一次访问到底经过了哪些关键步骤。

---

## 本组内容

- `从输入 URL 到页面显示`
  串起 DNS、TCP、TLS、HTTP 和浏览器处理流程。

---

## 阅读重点

- 先建立端到端链路视角
- 再把 DNS、连接、安全和渲染放到同一流程里理解
